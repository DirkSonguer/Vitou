Input: direction, speed

// check if speed is allowed
if (Player.CurrentTankEntity.CanMoveComponent.speed < Input.speed) {
	return ERROR;
}

// check for obstacles
if (..) ..

// calculate new position
Player.CurrentTankEntity.PositionComponent.x = ..
Player.CurrentTankEntity.PositionComponent.y = ..

return OK;